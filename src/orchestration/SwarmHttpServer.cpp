#include "SwarmHttpServer.h"
#include "ProverSwarm.h"

void SwarmHttpServer::attach(ProverSwarm& /*swarm*/) {
    // TODO: integrate with AILEEWebServer to expose /prover/register, etc.
    // For now, this is a stub so the build succeeds.
}
