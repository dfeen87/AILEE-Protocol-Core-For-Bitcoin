services:
  - type: web
    name: ailee-node
    env: docker
    plan: starter
    envVars:
      - key: PORT
        value: "8080"
      - key: AILEE_MODE
        value: "production"
      - key: AILEE_LOG_LEVEL
        value: "info"
